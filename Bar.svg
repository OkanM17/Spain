<svg width="200" height="200" viewBox="0 0 200 200"
     xmlns="http://www.w3.org/2000/svg">

  <!-- Linke Hälfte: Barcelona Blau -->
  <path d="M100,10 A90,90 0 0,0 100,190 L100,100 Z" fill="#004D98" />

  <!-- Rechte Hälfte: Barcelona Burgund/Rot -->
  <path d="M100,10 A90,90 0 0,1 100,190 L100,100 Z" fill="#A50044" />

  <!-- Kreisrand -->
  <circle cx="100" cy="100" r="90" fill="none" stroke="black" stroke-width="4" />

  <!-- Text: zwei Hälften übereinander -->
  <!-- Linke Texthälfte (blau) -->
  <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle"
        font-family="GS" font-size="60" font-weight="bold"
        fill="#A50044" clip-path="url(#left)">
    BAR
  </text>

  <!-- Rechte Texthälfte (burgund) -->
  <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle"
        font-family="GS" font-size="60" font-weight="bold"
        fill="#004D98" clip-path="url(#right)">
    BAR
  </text>

  <!-- Clip-Bereiche -->
  <defs>
    <clipPath id="left">
      <rect x="0" y="0" width="100" height="200" />
    </clipPath>

    <clipPath id="right">
      <rect x="100" y="0" width="100" height="200" />
    </clipPath>
  </defs>

</svg>
